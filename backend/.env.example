# Local Postgres (docker-compose at repo root)
DATABASE_URL=postgresql://socialpulse:socialpulse_dev@localhost:5432/socialpulse

# API
PORT=3001
BIND=127.0.0.1

# Day-boundary + display timezone (IANA name; UTC+4, no DST)
APP_TIMEZONE=Asia/Dubai
